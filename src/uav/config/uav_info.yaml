#判断是否载入该yaml文件
load_param_flag: 1

#判读是否有组名,0无组名，1有组名
group_flag: 1
# group_flag:0  启动  ns_flag:=g0  无组名
# group_flag:1  启动  ns_flag:=g1  有组名

model_name0: uav0
# tof端口名
tof_usb: /dev/tof
# tof波特率
baud_tof_rate: 115200  


# uav_info使用的参数
